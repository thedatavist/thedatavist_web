[build]
  command = """
    curl -LO https://quarto.org/download/latest/quarto-linux-amd64.deb && \
    mkdir -p /tmp/quarto && \
    dpkg -x quarto-linux-amd64.deb /tmp/quarto && \
    /tmp/quarto/opt/quarto/bin/quarto render
  """
  publish = "_site"

[build.environment]
  QUARTO_PRINT_STACK = "true"
